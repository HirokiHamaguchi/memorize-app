export interface Flag {
    id: number;
    iso: string;  // ISO country code (e.g., "ad", "ae")
    ja: string;
    flag: string;
    pos: string;
    url: string;
}

export interface Vocabulary {
    id: number;
    en: string;
    ja: string;
}