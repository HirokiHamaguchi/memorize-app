export interface Flag {
    id: string;
    code: string;  // ISO country code (e.g., "ad", "ae")
    ja: string;
    flag: string;
    pos: string;
}

export interface Vocabulary {
    id: string;
    en: string;
    ja: string;
}